<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quote Website</title>
    <style>
        textarea {
            width: 100%;
            height: 100px;
        }
    </style>
</head>
<body>
    <h1>Quote Website</h1>

    <h2>Add Quote</h2>
    <form action="/add" method="post">
        <label for="quote">Quote:</label><br>
        <textarea name="quote" id="quote" placeholder="Enter your quote" required></textarea><br>

        <label for="author">Author:</label><br>
        <input type="text" name="author" id="author" placeholder="Enter the author's name" required><br>

        <label for="date">Date:</label><br>
        <input type="date" name="date" id="date" required><br>

        <button type="submit">Add</button>
    </form>

    <h2>Quotes</h2>
    <ul>
        <% quotes.forEach(quote => { %>
            <li><%= quote["quote"] %> - <%= quote["author"] %> (<%= quote["date"] %>)</li>
        <% }); %>
    </ul>
</body>
</html>
